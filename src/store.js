import { writable } from 'svelte/store';

export const error = writable(undefined);

export const user = writable(undefined);
export const family = writable(undefined);
export const member = writable(undefined);
